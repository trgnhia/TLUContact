<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tlulogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tlulogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.TLUContact"
        tools:targetApi="31">

        <!-- Màn hình Welcome (launcher) -->
        <activity
            android:name=".activity.WelcomActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activity.CBGVActivity"
            android:exported="false" />

        <activity
            android:name=".activity.DBDVActivity"
            android:exported="false" />
    </application>

</manifest>
